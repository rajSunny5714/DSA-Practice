#include <stdio.h>
int main() {
    char str[100], str2[100];
    int j=0;
    scanf("%[^\n]",str);
    for(int i=0; str[i]!='\0'; i++){
        if(str[i]!=' ') str2[j++]=str[i];
    }
    str2[j]='\0';
    printf("%s",str2);
    return 0;
}